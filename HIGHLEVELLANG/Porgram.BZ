var InputText = "" : string
Func Reset

	port::Write CHR '\'
	port::Write CHR '\n'
	InputText = ""

endFunc

Func GetLine
	
	GetLine InputText

	if InputText == "ent"
		Enter
	end
	if InputText == "back"
		BackSpace
	end
	if InputText == "esc"
		Escape
	end

	:do something with the InputText in some func or something 

endFunc

Func Enter

endFunc

Func BackSpace

endFunc

Func Escape

endFunc